# 需求文档

更新：2026-09-26｜实现基线：0.2.0（分层分类迭代）

## 1. 目标与范围

Bei File Tidying 是面向个人用户的 Java 命令行文件整理助手。用户指定感知根目录和待整理目录，程序参考目录结构生成逐文件建议，经确认后执行，并支持撤销最近一次整理。

- `sense.root`：提供目录结构上下文，也是整理目的地的范围。
- `target.dir`：本次扫描文件的目录，必须是感知根目录的子目录；递归处理其中的文件。
- 分类合理、结构清晰是首要原则；合适时复用已有目录，也允许新建分类目录。
- 本版为手动启动、执行一次的程序；持续监听、GUI、数据库、多次历史撤销暂不纳入范围。

## 2. 功能要求与验收

| 编号 | 要求 | 验收标准 | 当前状态 |
| --- | --- | --- | --- |
| F01 | 配置与启动 | IDEA / 命令行均可启动；无效根目录或目标目录在分析前报错 | 已实现 |
| F02 | 感知与扫描 | 树形显示目录，默认深度 3；递归收集待整理文件，跳过点号开头的文件 | 已实现，有边界限制 |
| F03 | AI 整理建议 | 每个文件显示目的地、理由、状态及新建目录提示；允许保留原位 | 已实现 |
| F04 | 批量与进度 | 默认每批 10 个文件、最多 2 批并发；输出扫描、请求、结果和失败原因 | 已实现，待规模验证 |
| F05 | 确认执行 | 输入大写 `APPLY` 后才创建目录并移动；其他输入取消，`UNDO` 除外 | 已实现 |
| F06 | 移动保护 | 拒绝越界目的地；同名文件不覆盖；失败项显示原因 | 已有基础检查，待加固 |
| F07 | 空目录清理 | 可关闭；仅清理本次成功移动后变空的目标子目录，保留目标目录本身 | 已实现 |
| F08 | 最近一次撤销 | `--undo-last` 恢复文件及被删目录，清理新建空目录；冲突项报告并保留 | 已实现，异常场景待补测 |
| F09 | API 与本地模式 | 支持第三方兼容接口；密钥为空时明确提示并按扩展名分类 | 已实现 |
| F10 | 按需使用 AI | 明确规则优先，其余先发元数据；仅应模型要求补充文本，每文件最多 1,200 字符 | 已实现 |
| F11 | 消耗控制 | 默认单次运行最多 20 次请求、每次提示词最多 12,000 字符；未解决项仅补请求一次；显示实际统计 | 已实现 |
| F12 | 文件组 | 明确编号系列生成一条组建议；模型确认一致后逐成员校验，异常回退逐文件 | 已实现保守规则 |
| F13 | 分类缓存 | 有效 AI 建议可复用；文件、目录、模型或密钥变化时失效；`--refresh` 强制重算 | 已实现 |

## 3. 约束与当前限制

- Java 17、Maven Wrapper；项目文件路径使用英文，真实密钥、构建产物及操作历史不入库。
- 默认模型为 `gpt-6-luna`，思考强度 `medium`；需使用密钥所属平台的接口地址。
- AI 接收相对路径、扩展名、大小和相关目录索引；文本按需补充。PDF、Office、图片目前只分析文件名及元数据。
- `max.file.mb` 控制是否读取文本内容；超限文件仍可按元数据分类。
- AI 目录索引独立于终端展示深度，优先选择名称相关分支，最多 2,000 字符；检索尚不具备语义理解，隐藏子目录扫描规则仍需完善。
- 计划、执行和撤销会拒绝静态符号链接路径；并发替换路径、响应 JSON 严格校验与撤销异常恢复仍需加固。
- 暂无大规模耗时或成功率承诺；自动化覆盖与真实 AI 结果见[测试文档](testing.md)。

## 4. 本轮交付边界

本轮在现有分层分类上加入保守文件组与分类缓存；组内语义依赖模型确认，缓存以元数据及前段摘要识别变化。后续优先级与验收门槛见[整体项目计划](project-plan.md)。
